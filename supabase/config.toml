project_id = "ucbqrqyvepoxpuilfgjv"

[auth]
enabled = true
# Session timeout in seconds (2 minutes = 120 seconds)
jwt_expiry = 120
# Refresh token expiry (keep longer for development)
refresh_token_rotation_enabled = true
# Enable automatic token refresh
enable_refresh_token_rotation = true

[storage.avatars]
public = true
