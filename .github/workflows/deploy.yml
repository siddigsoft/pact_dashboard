name: Deploy to VPS Production

on:
  push:
    branches:
      - master_backup

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Deploy to VPS via SSH (hardcoded key)
        uses: appleboy/ssh-action@v1.2.0
        with:
          host: 209.38.78.38
          username: deploy
          key: |
            -----BEGIN OPENSSH PRIVATE KEY-----
            
            -----END OPENSSH PRIVATE KEY-----
          script: |
            set -e
            echo "🚀 Deploying PACT Frontend (static)"
            cd /home/deploy/pact_dashboard

            # Ensure Git can modify files (no sudo needed)
            chmod -R u+w .

            # Pull latest code
            git fetch origin
            git reset --hard origin/master_backup
            git clean -fd

            # Rebuild
            npm install
            npm run build

            # Set safe permissions for Nginx (world-readable)
            find dist -type d -exec chmod 755 {} \;
            find dist -type f -exec chmod 644 {} \;

            echo "✅ Deployment completed successfully"